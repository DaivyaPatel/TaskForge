# TaskForge
Hierarchical Task Management Platform.

## Setup
1. Make sure you have Node 20+ and pnpm installed.
2. Run `pnpm install` in the root directory.
3. Copy `.env.example` to `.env` inside the `server/` folder and fill in your variables.
4. Run `pnpm run dev` to start both the frontend and backend.